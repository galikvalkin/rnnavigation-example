import { ROUTE_NAMES } from './constants';

const ROOT_STACK = {
  stack: {
    children: [
      {
        component: {
          name: ROUTE_NAMES.ROOT,
        },
      },
    ],
  },
};

export { ROOT_STACK };
