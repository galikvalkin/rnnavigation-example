import { ROUTE_NAMES } from './constants';

const MODAL = {
  stack: {
    children: [
      {
        component: {
          name: ROUTE_NAMES.MODAL,
        },
      },
    ],
  },
};

export { MODAL };
